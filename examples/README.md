# examples/

Place example files here to demonstrate the tool:

- `video.mp4` — a short Synthesia clip (a few seconds is enough)
- `transcription.mid` — the corresponding MIDI from audio transcription
- `output.mid` — the expected output (two-track hand-separated MIDI)

A short clip showing a few bars with clearly separate green/blue notes
makes the best demo example.
